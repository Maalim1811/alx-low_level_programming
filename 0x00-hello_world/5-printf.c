#include <stdio.h>

/**
 * main - Print a string literal.
 *
 * Description: The function prints out the string literal
 * It prints the string literal using the function printf
 * The string literal is : 'with proper grammar, but the outcome is a piece of
 * art,'
 * Return: returns int 0 showing success.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
